apiVersion: apps/v1
kind: Deployment
metadata:
  name: githoster-prod-dpl
spec:
  replicas: 3
  selector:
    matchLabels: 
      app: githoster-test
  template:
    metadata:
      name: githoster-test-pod
      labels:
        app: githoster-test
    spec:
      containers:
        - name: githoster-test
          image: githoster/githoster-app


---

apiVersion: v1
kind: Service
metadata:
  name: githoster-test-srv
spec: 
  type: NodePort
  selector:
    app: githoster-test
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007